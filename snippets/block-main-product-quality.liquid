{%- capture content -%}
  {{- product.metafields.quality.label | escape | strip_newlines | strip -}}
{%- endcapture -%}
{%- capture content -%}{% if content != '' %}"{{content}}"{% endif %}{%- endcapture -%}
<script id="inline-json-block-product-quality" type="application/json">
{% raw %}{{% endraw %}
"id": "{{ block.id }}",
"heading": "{{ block.settings.heading }}",
"content": [{{ content }}]
{% raw %}}{% endraw %}
</script>

{% comment %} {% assign detailId = 'Details-' | append: block.id | append: '-' | append: section.id %}
{% assign accordionId = 'ProductAccordion-' | append: block.id | append: '-' | append: section.id %}
{% capture content %}
  {{ product.metafields.quality.label | newline_to_br }}
{% endcapture %}
{% render 'collapsible_tab',
  attr: block.shopify_attributes,
  detailId: detailId,
  icon: block.settings.icon,
  heading: block.settings.heading,
  accordionId: accordionId,
  content: content
%} {% endcomment %}